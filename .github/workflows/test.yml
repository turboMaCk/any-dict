name: "Test"

on:
  pull_request:
  push:

jobs:
  runs-on: ubuntu-latest
  steps:
    - uses: sparksp/elm-format-action@v1
    - uses: jorelali/setup-elm@v2
      with:
        elm-version: 0.19.1
    - run: elm make
    - run: elm-verify-examples -r
